{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idp</th>
                <td>{{ panier.idp }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ panier.quantite }}</td>
            </tr>
            <tr>
                <th>Nomproduit</th>
                <td>{{ panier.nomproduit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_panier_index') }}">retour a la liste</a>

    <a href="{{ path('app_panier_edit', {'idp': panier.idp}) }}">edit</a>

    {{ include('panier/_delete_form.html.twig') }}
{% endblock %}
